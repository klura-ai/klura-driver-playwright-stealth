{
  "name": "@klura/driver-playwright-stealth",
  "version": "0.1.0",
  "publishConfig": {
    "access": "public"
  },
  "description": "Stealth Playwright driver for klura — adds puppeteer-extra-plugin-stealth fingerprint patches",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "klura",
    "playwright",
    "stealth",
    "driver"
  ],
  "license": "BUSL-1.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "peerDependencies": {
    "@klura/runtime": ">=0.1.0",
    "playwright": ">=1.50.0"
  },
  "dependencies": {
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@klura/runtime": "file:../runtime",
    "playwright": "^1.50.0",
    "typescript": "^6.0.2"
  }
}
