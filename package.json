{
  "name": "@klura/driver-playwright-stealth",
  "version": "0.1.0",
  "publishConfig": {
    "access": "public"
  },
  "description": "Stealth Playwright driver for klura — adds puppeteer-extra-plugin-stealth fingerprint patches",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "scripts/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "patch:stealth": "node scripts/patch-pw-initscripts.js",
    "postinstall": "node scripts/patch-pw-initscripts.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "klura",
    "playwright",
    "stealth",
    "driver"
  ],
  "license": "BUSL-1.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "peerDependencies": {
    "@klura/runtime": ">=0.1.0",
    "playwright": ">=1.50.0"
  },
  "dependencies": {
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "rebrowser-playwright": "^1.52.0"
  },
  "devDependencies": {
    "@klura/runtime": "file:../runtime",
    "@types/node": "^25.5.2",
    "playwright": "^1.50.0",
    "typescript": "^6.0.2"
  }
}
